{
  "name": "server-2",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server -w db.json -p 3002",
  "build": "gulp build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-cssnano": "^2.1.3",
    "gulp-uglify": "^3.0.2",
    "json-server": "^0.17.3",
    "uglify": "^0.1.5"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
